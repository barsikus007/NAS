http:
  middlewares:
    authelia:
      forwardAuth:
        address: 'http://authelia:9091/api/verify?rd=https://auth.{{ env "HOST" }}'
        trustForwardHeader: true
        authResponseHeaders: Remote-User,Remote-Groups,Remote-Name,Remote-Email
    secure-headers:
      headers:
        # stsSeconds: 31536000
        # stsIncludeSubdomains: true
        contentTypeNosniff: true
        browserXssFilter: true
        #! keep on a single line: a trailing newline makes the value invalid and Go silently drops the header on HTTP/2
        contentSecurityPolicy: 'frame-ancestors ''self'' https://{{ env "HOST" }} https://www.{{ env "HOST" }}'
        referrerPolicy: "same-origin"
    gzip:
      compress: {}
    crowdsec:
      plugin:
        crowdsec-bouncer:
          enabled: true
          crowdseclapikey: '{{ env "BOUNCER_KEY_TRAEFIK" }}'
